<!DOCTYPE html>
<html lang="en">

{{>head}}

<body>
    {{>nav}}


    <div class="container-fluid">
        <div class="row">
            <div class="wrapper">
                {{>sidebar}}
                <div class="content">
                    {{{ body }}}
                </div>
            </div>
        </div>
    </div>

     <script>
        $(document).ready(function () {
            $(".push_menu").click(function () {
                $(".wrapper").toggleClass("active");
                $(this).find('span').toggleClass('glyphicon-chevron-right').toggleClass('glyphicon-chevron-left');
            });
        });

        function sendMessageToIframe() {
            var iFrame = document.getElementById('editablewindow');
            iFrame.contentWindow.postMessage("message", "http://localhost");
            //alert("Send message function called....");
        }

		function responseveIframe() {
  $('iframe').height(
    $('iframe').attr("height") / $('iframe').attr("width") * $('iframe').width()
  );
}

responseveIframe();

$(window).resize(function() {
  responseveIframe();
});


    </script>
</body>

</html>
